<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.semi_project.dao.UserDAO">

    <resultMap id="userResult" type="com.example.semi_project.dto.UserSelectDTO">
        <id property="code" column="USER_CODE"/>
        <result property="email" column="EMAIL"/>
        <result property="regist" column="REGIST_DATE"/>
        <result property="status" column="USER_STATUS"/>
    </resultMap>

    <select id="selectAllUser" resultMap="userResult">
        SELECT
            U.USER_CODE,
            U.EMAIL,
            UI.REGIST_DATE
          FROM TBL_USER U
          JOIN TBL_USER_INFO UI ON U.USER_CODE = UI.USER_CODE
         WHERE UI.USER_STATUS = 'Y'
    </select>
</mapper>